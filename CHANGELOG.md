# 更新日志

本项目的所有重要变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v1.4.0] - 2026-08-18

### 新增

- **回声书签商店**（`/刮商店`）：无参数展示货架，带参数直接购买：
  - 方斯：1 书签 = 1,000 方斯；
  - 各档追加额度：2 万档 10 / 3 万档 30 / 5 万档 50 书签每张（`卡2` / `卡3` / `卡5`）；
  - 保底护符（当日有效，每日限购一种，大手/后门互斥）。
- **保底护符系统**：
  - **玛门的大手**（1,000 书签）：今日基础额度购卡未中奖全额返还成本；
  - **猫丸的后门**（1,500 书签）：今日基础额度购卡中奖低于成本时返还差额。
  - 护符仅对基础限购额度内购卡生效。
- **`/刮全部`** 指令：一次购买所有档位今日基础限购剩余的全部额度，每张卡各占一个合并转发节点。

### 变更

- 回声书签由「购卡无条件累计、未来可兑换」调整为「**仅基础限购额度购卡累计**，可在商店兑换方斯 / 追加额度 / 护符」；追加或管理员发放的额外额度购卡不再赠送书签。

### 重构

- 代码拆分：`config.py`（常量配置）、`card_utils.py`（纯工具函数）、`leaderboard_template.py`（排行榜模板）迁移至 `core/` 子包，根目录新增 `__init__.py`；`main.py` 通过相对导入（`from .core import ...`）引用。
- 删除未使用的 `assets/leaderboard.css`。

## [v1.3.1] - 2026-08-14

### 变更

- 清理旧版（v1.2.0）遗留的单档 `daily_bought` / `daily_extra` 统计字段，每日限购统一由按档位字段（`daily_bought_by_tier` / `daily_extra_by_tier`）驱动。

## [v1.3.0] - 2026-08-14

### 新增

- 新增 **2 万档**（`/刮刮乐2`）与 **3 万档**（`/刮刮乐3`）刮刮卡，售价分别为 20,000 / 30,000 方斯；新增 `/刮刮乐5` 显式选择 5 万档，`/刮刮乐` 保持默认 5 万档（向后兼容）。
- 各档卡使用独立的单格奖金档位（`CELL_PRIZE_TIERS`）与总奖金概率分布，数据来自游戏实际提取。
- 更新 5 万卡总奖金概率分布，对齐游戏实际数据。
- LLM 工具 `scratch_ntc_card` 新增 `tier` 参数（20000 / 30000 / 50000，默认 50000），可自然语言指定卡档位。
- 各档卡**独立每日限购**：2 万卡 20 张 / 3 万卡 30 张 / 5 万卡 50 张，对齐游戏实际数据；管理员发卡（`/刮发卡`）与 LLM `give_cards` / 申请流程可按档位发放额外额度，`/刮余额` 按档显示今日剩余。
- 5 万档刮刮卡改为 **12 格（3 × 4）**，2 万 / 3 万档保持 15 格（3 × 5）；格子数与排布按档位独立配置。
- 新增道具系统「回声书签」：2 万卡每张 +1、3 万卡每张 +3、5 万卡每张 +5，购卡自动累计（`/刮余额` 可查），为未来兑换预留。
- 奖金拆分算法升级：引入按 10000 缩放的 DP 可表示性校验，保证只要存在标准档位组合就拆出纯档位结果，消除极端金额下的非档位兑底格。

### 变更

- 卡片生成重构为多档位架构（`CARD_TYPES`）：`_decompose_prize` 按各档单格档位动态计算不可达剩余金额，`_generate_card` 按档位抽奖与拆分。
- 购卡结果消息标题显示档位标签（如「刮刮乐 · 2万」）。

## [v1.2.0] - 2026-08-10

### 新增

- 接入 LLM 自然语言指令（`@filter.llm_tool`），支持大模型根据用户意图自动触发：
  - 刮卡（`scratch_ntc_card`）、每日福利（`daily_pension`）、查余额（`check_scratch_balance`）；
  - 发钱/发卡（`give_money` / `give_cards`）按说话者身份自动分流：管理员直接发放，普通用户自动转为申请（目标强制为自己）；
  - 管理员审批（`admin_approve_request` / `admin_reject_request`），支持直接引用机器人发的申请消息回复「同意/批准/拒绝」即可处理，也可带编号或唯一待审批时直接回复；
  - 查看待审批清单（`list_pending_requests`）。
- 新增 `enable_llm_tools` 总开关（默认开启）：关闭后大模型将无法触发任何 LLM 工具，精确指令（`刮刮乐`、`刮余额` 等）不受影响。

### 变更

- 管理员判定统一使用 AstrBot 后台配置的 `admins_id`（`event.is_admin()`），不再单独维护群内管理员判断。
- 成员申请记录持久化到 `pending_requests.json`，重启不丢失。
- 合并发钱/发卡 LLM 工具（原 `admin_give_*` / `request_give_*` 共 4 个合并为 `give_money` / `give_cards`），按角色自动分流，减少意图识别歧义。

## [v1.1.0] - 2026-08-08

### 新增

- 富爪榜新增「排除长时间未抽卡用户」功能：超过 `leaderboard_inactive_days`（默认 7 天，可配置，0 表示不过滤）未抽卡的用户不会出现在 `富爪榜` 中。
- 每次 `/刮刮乐` 记录用户最后抽卡日期（`last_scratch_date`），作为活跃度判定依据。

### 优化

- 多张购卡（`/刮刮乐 N`）的汇总信息改为每项一条独立消息，并调整展示顺序（本次中奖情况 → 本次收入 → 累计收入 → 今日剩余 → 当前余额），去掉汇总标题。
- 排行榜用户名不再在代码中截断长度，由 HTML 模板渲染时自动省略（`text-overflow: ellipsis`）。
- 排行榜（`富爪榜` / `富爪日榜`）文档说明补充活跃度过滤规则。

### 文档

- README 更新：新增 `leaderboard_inactive_days` 配置项说明。

## [v1.0.0] - 2026-08-04

首个正式发布版本。

### 新增

- 购买并刮开刮刮卡，支持一次多张（合并转发展示）。
- 基于异环实际数据构建的精确概率系统，先按概率抽取总奖金，再分解到 15 格（3 × 5）。
- 每日限购（默认 60 张，可配置），支持管理员发放额外购卡额度（`/刮发卡`）。
- 每日随机领取方斯（`/刮取钱`），默认 30/50/70/100 万四档，周期内顺序随机、发完重洗，档位可自定义。
- 货币系统：初始余额 300 万方斯，支持 `/刮转账` 转账，管理员 `/刮发钱` 发钱。
- 排行榜：
  - `/富爪榜` 累计盈亏排行榜；
  - `/富爪日榜` 今日盈亏排行榜。
  - 通过 AstrBot 文转图（HTML 渲染）生成 NTE 风格榜单图片，渲染失败自动回退纯文本榜。
  - 依赖 NapCat API 获取群成员列表与群名片，实现跨群隔离与昵称显示。
- 个人数据查询（`/刮余额`）：余额、累计盈亏、中奖率、购买/中奖次数等。
- 帮助指令（`/刮刮乐帮助`）。
