# 更新日志

本项目的所有重要变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v1.1.0] - 2026-08-08

### 新增

- 富爪榜新增「排除长时间未抽卡用户」功能：超过 `leaderboard_inactive_days`（默认 7 天，可配置，0 表示不过滤）未抽卡的用户不会出现在 `富爪榜` 中。
- 每次 `/刮刮乐` 记录用户最后抽卡日期（`last_scratch_date`），作为活跃度判定依据。

### 优化

- 多张购卡（`/刮刮乐 N`）的汇总信息改为每项一条独立消息，并调整展示顺序（本次中奖情况 → 本次收入 → 累计收入 → 今日剩余 → 当前余额），去掉汇总标题。
- 排行榜用户名不再在代码中截断长度，由 HTML 模板渲染时自动省略（`text-overflow: ellipsis`）。
- 排行榜（`富爪榜` / `富爪日榜`）文档说明补充活跃度过滤规则。

### 文档

- README 更新：新增 `leaderboard_inactive_days` 配置项说明。

## [v1.0.0] - 2026-08-04

首个正式发布版本。

### 新增

- 购买并刮开刮刮卡，支持一次多张（合并转发展示）。
- 基于异环实际数据构建的精确概率系统，先按概率抽取总奖金，再分解到 15 格（3 × 5）。
- 每日限购（默认 60 张，可配置），支持管理员发放额外购卡额度（`/刮发卡`）。
- 每日随机领取方斯（`/刮取钱`），默认 30/50/70/100 万四档，周期内顺序随机、发完重洗，档位可自定义。
- 货币系统：初始余额 300 万方斯，支持 `/刮转账` 转账，管理员 `/刮发钱` 发钱。
- 排行榜：
  - `/富爪榜` 累计盈亏排行榜；
  - `/富爪日榜` 今日盈亏排行榜。
  - 通过 AstrBot 文转图（HTML 渲染）生成 NTE 风格榜单图片，渲染失败自动回退纯文本榜。
  - 依赖 NapCat API 获取群成员列表与群名片，实现跨群隔离与昵称显示。
- 个人数据查询（`/刮余额`）：余额、累计盈亏、中奖率、购买/中奖次数等。
- 帮助指令（`/刮刮乐帮助`）。
